<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
	<select id="" parameterType="productReviewVO">
		select from plogging.tbl_product_review
		(user_id, product_number, review_detail, user_review_number)
		VALUES(#{userId}, #{productNumber}, #{reviewDetail}, #{userReviewNumber})
	</select>
</mapper>